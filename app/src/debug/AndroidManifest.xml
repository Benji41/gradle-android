<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        >
        <!-- overrides the node completed merged with the main sourceset manifest file. changing the theme on this example-->
        <activity
            android:name="com.reynoso.debug.MainActivity"
            android:theme="${theme}"
            android:exported="true"
            tools:node="replace"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>